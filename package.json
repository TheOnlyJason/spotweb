{
  "name": "spotweb",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "three": "^0.175.0"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.42.2",
    "vite": "^6.3.5",
    "wrangler": "^4.104.0"
  },
  "engines": {
    "node": ">=20"
  }
}
